services:
  app:
    build: .
    container_name: rcda-app
    working_dir: /app
    command: npm run dev:renderer -- --host 0.0.0.0 --port 5173
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - rcda_node_modules:/app/node_modules

volumes:
  rcda_node_modules:
